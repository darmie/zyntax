[package]
name = "zrtl_net"
version = "0.1.0"
edition = "2021"
description = "ZRTL Network plugin - TCP/UDP networking"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
zrtl = { path = "../../sdk/zrtl" }
