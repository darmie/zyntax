module Test;

model User {
    var id: int,
    var name: string,

    new(id: int, name: string) {
        this.id = id
        this.name = name
    }

    function greet() -> string {
        return "Hello"
    }
}
