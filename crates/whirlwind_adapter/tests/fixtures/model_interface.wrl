module Test;

// Model (class) and Interface test
interface Drawable {
    function draw() -> void
}

model Circle {
    var radius: float,

    new(r: float) {
        this.radius = r
    }

    function draw() -> void {
        // Draw circle logic
    }

    function area() -> float {
        return 3.14159 * this.radius * this.radius
    }
}
